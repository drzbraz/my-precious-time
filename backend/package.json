{
  "name": "precious-time-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute precious-time --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute precious-time --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
